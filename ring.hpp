#pragma once

#ifndef RING_HPP__
#define RING_HPP__

#include <uuid.hpp>

#include <unordered_set>

namespace ring
{

using peers = std::unordered_set<uuid::uuid>;




} // namespace peers

#endif // RING_HPP__
